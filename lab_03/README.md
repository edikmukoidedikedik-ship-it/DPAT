# Лабораторна робота №3: Візуалізація даних
**Виконав:** Мукоїд Едуард ФБ-44

## Опис завдання
Метою роботи є вибір унікального багатовимірного датасету, проведення його очищення та побудова 5-8 різнопланових графіків для виявлення закономірностей у даних.

Для роботи обрано датасет **Communities and Crime** з репозиторію UCI.
* **Характеристики**: Multivariate.
* **Атрибути**: Categorical, Integer, Real.
* **Пропуски**: Присутні (оброблені на етапі Data Cleaning).

## Системні вимоги та налаштування
* **Інтерпретатор**: Python 3.10+
* **ОС**: Windows 10/11 (PowerShell)
* **Основні бібліотеки**: `pandas`, `numpy`, `seaborn`, `matplotlib`, `jupyter`.

## Структура проєкту
* lab_03.ipynb — основний файл з кодом, візуалізаціями та висновками.
* data/ — папка для зберігання датасету.
* requirements.txt — список необхідних пакетів.
* .gitignore — ігнорування системних файлів, кешу та віртуального середовища.

### Інструкція із запуску
1. Перейдіть у директорію лабораторної роботи:
   ```powershell
   cd lab_03

2. Створіть та активуйте віртуальне середовище:
    ```bash
    python -m venv venv
    .\venv\Scripts\activate

Примітка: Якщо виконання скриптів заблоковано, виконайте: Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser

3. Встановіть необхідні залежності:
    ```bash
    pip install -r requirements.txt

4. Запустіть Jupyter Notebook: 
    ```bash
    jupyter notebook